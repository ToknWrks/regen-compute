/**
 * Beta banner + feedback widget — DISABLED.
 *
 * Functions return empty strings so all call sites continue to work
 * without modification. Remove the imports and interpolations later
 * if you want to fully clean up.
 */

export function betaBannerCSS(): string {
  return "";
}

export function betaBannerHTML(): string {
  return "";
}

export function betaBannerJS(): string {
  return "";
}
